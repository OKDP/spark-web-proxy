apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark-history-web-proxy.fullname" . }}
  labels:
    {{- include "spark-history-web-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark-history-web-proxy.selectorLabels" . | nindent 4 }}
